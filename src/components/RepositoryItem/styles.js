import styled from 'styled-components';

export const Container = styled.div`
  .card-title {
    color: #36393f;
    width: 100%;
    margin: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .card-text {
    width: 100%;
    height: 50px;
    font-size: 14px;
    line-height: 18px;
    overflow: hidden;
    text-overflow: ellipsis;
  }
`;
